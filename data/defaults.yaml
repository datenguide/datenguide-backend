na_values: ['x', '-', '.']
encoding: latin1
delimiter: ';'
dtype: str
# filter:
#   _id: 'lambda x: len(x) == 5'
post_maps:
  _date:
    target: _year
    func: 'lambda x: "year:%s" % x[-4:] if x else x'
